<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fruits and Vegetables Home</title>
    
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    
    <!-- Custom Styles -->
    <style>
        /* Full-page background image */
        body {
            background-image: url('https://thumbs.dreamstime.com/z/fruit-vegetable-textures-9966904.jpg');
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
            font-family: Arial, sans-serif;
            color: #fff; /* White text color for readability */
        }

        /* Transparent overlay for content readability */
        .content-overlay {
            background-color:white; /* Semi-transparent dark overlay */
            padding: 50px;
            border-radius: 10px;
        }

        /* Button styling */
        .nav-button {
            font-size: 16px;
            padding: 10px 20px;
            margin: 5px;
            border-radius: 5px;
        }

        /* Center main content */
        .main-content {
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            text-align: center;
        }
        .nav-button {
    font-size: 16px; /* Adjust the font size */
    padding: 10px 20px; /* Add padding to make buttons bigger */
    margin: 5px; /* Add space between buttons */
    border-radius: 5px; /* Make the corners rounded */
    box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.3); /* Add shadow for better visibility */
    opacity: 0.9; /* Make the buttons slightly transparent for a modern look */
    transition: all 0.3s ease; /* Smooth transition effect */
}

.nav-button:hover {
    opacity: 1; /* Full opacity when hovering */
    transform: translateY(-3px); /* Slight lift effect on hover */
}

.btn-primary {
    background-color: rgba(0, 123, 255, 0.8); /* Semi-transparent primary color */
    border: none;
}

.btn-success {
    background-color: rgba(40, 167, 69, 0.8); /* Semi-transparent success color */
    border: none;
}

.btn-warning {
    background-color: rgba(255, 193, 7, 0.8); /* Semi-transparent warning color */
    border: none;
}

    </style>
</head>
<body>
    <!-- Navigation Bar with Bootstrap buttons -->
    <div style="position: absolute; top: 20px; right: 20px;">
        <a href="/fruits">
            <button class="btn btn-primary nav-button">Fruits</button>
        </a>
        <a href="/vegetables">
            <button class="btn btn-success nav-button">Vegetables</button>
        </a>
        <a href="/cart">
            <button class="btn btn-warning nav-button">Cart</button>
        </a>
    </div>

    <!-- Home Page Content with Overlay -->
    <div class="main-content">
        <div class="content-overlay">
            <h1 style="background: linear-gradient(90deg, #ff7e5f, #feb47b, #86a8e7, #d4a5a5); 
               -webkit-background-clip: text; 
               background-clip: text; 
               -webkit-text-fill-color: transparent; 
               font-size: 3em; 
               font-weight: bold;">
        Welcome to the Hellth-Wellth Store!
    </h1>
    <p style="background: linear-gradient(90deg, #ff7e5f, #feb47b, #86a8e7, #d4a5a5); 
              -webkit-background-clip: text; 
              background-clip: text; 
              -webkit-text-fill-color: transparent; 
              font-size: 1em; 
              font-weight: bold;">
        Explore our fresh collection of fruits and vegetables. Click on the buttons above to browse.
    </p>
              
        </div>
    </div>

    <!-- Bootstrap JS and dependencies -->
    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>